# ===================== Secrets (NEVER commit) =====================
.env
.env.*
!.env.example

# ===================== CI / Dev artifacts =====================
# ===================== Internal deploy / ops tooling =====================
# 含内部服务器地址与部署凭据，不对外公开
scripts/_deploy/

# ===================== Credential-bearing compose files =====================
docker-compose.prod.yml
docker-compose.deploy.yml
infra/docker-compose.yml

# ===================== Python =====================
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
*.egg

# ===================== Node / frontend =====================
node_modules/
studio/dist/
dist/
build/
.vite/

# ===================== Data runtime artifacts (seed data kept) =====================
*.db
*.sqlite
*.sqlite3
data/*.db
# 企业入驻 Vault 运行时产物（含加密密钥 vault.key，绝不入库）
data/vault.key
data/credential_vault.json
data/enterprise_profiles.json

# ===================== GitHub token（仅本地，绝不明文入库） =====================
# 🔴 安全红线：真实 GitHub PAT 只许放这里（gitignored），绝不进提交历史
scripts/_deploy/.gh_token

# ===================== Logs =====================
*.log
logs/

# ===================== OS / Editor =====================
.DS_Store
Thumbs.db
.vscode/
.idea/
.workbuddy/

# ===================== Internal ops / WeChat publishing (not open-source) =====================
article_v20.md
cover_v20.jpg
cover_risk_gov.jpg
scripts/wechat_upload.py
scripts/publish_v20_draft.py
scripts/publish_risk_gov_draft.py
scripts/gen_cover_v20.py
scripts/gen_cover_risk_gov.py

# ===================== Local frontend config =====================
studio/vite.local.ts
outputs/
_sync.bundle

# ===================== 部署/恢复临时产物 & scratch 脚本（不入库） =====================
*.bundle
tests/_tmp_*.py
studio/tsconfig.tsbuildinfo

# ===================== 本地垃圾 / 构建QA / 备份目录（绝不入库） =====================
.trash_local/
.pptxqa/
.tmp_mobile_shots/
docs/_qa/
studio/dist_old_bak_*/
studio/dist_verify/
studio/.trash_local/
gh_maintain_run.py

# ===================== 案例库锚定 Vault（明文真名·绝不入库） =====================
src/agents/case_curator/_real_anchor_vault.json

# ===================== Office 二进制物料（本地保留，不入库） =====================
docs/智衍EvolvIQ_25个工业智能分身*
docs/智衍EvolvIQ_系统介绍_上海铁路通信有限公司*
