# ===================== Secrets (NEVER commit) =====================
.env
.env.*
!.env.example

# ===================== CI / Dev artifacts =====================
# ===================== Internal deploy / ops tooling =====================
# 含内部服务器地址与部署凭据，不对外公开
scripts/_deploy/

# ===================== Credential-bearing compose files =====================
docker-compose.prod.yml
docker-compose.deploy.yml
infra/docker-compose.yml

# ===================== Python =====================
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
*.egg

# ===================== Node / frontend =====================
node_modules/
studio/dist/
dist/
build/
.vite/

# ===================== Data runtime artifacts (seed data kept) =====================
*.db
*.sqlite
*.sqlite3
data/*.db
# 企业入驻 Vault 运行时产物（含加密密钥 vault.key，绝不入库）
data/vault.key
data/credential_vault.json
data/enterprise_profiles.json

# ===================== GitHub token（仅本地，绝不明文入库） =====================
# 🔴 安全红线：真实 GitHub PAT 只许放这里（gitignored），绝不进提交历史
scripts/_deploy/.gh_token

# ===================== Logs =====================
*.log
logs/

# ===================== OS / Editor =====================
.DS_Store
Thumbs.db
.vscode/
.idea/
.workbuddy/

# ===================== Internal ops / WeChat publishing (not open-source) =====================
article_v20.md
cover_v20.jpg
cover_risk_gov.jpg
scripts/wechat_upload.py
scripts/publish_v20_draft.py
scripts/publish_risk_gov_draft.py
scripts/gen_cover_v20.py
scripts/gen_cover_risk_gov.py

# ===================== Local frontend config =====================
studio/vite.local.ts
outputs/
_sync.bundle
