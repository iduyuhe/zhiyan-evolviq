# 智衍 EvolvIQ · 产品开发执行计划（v21.0 起）

> 版本：v1.0 · 2026-07-26 · 状态：依据战略收口的可执行开发计划（本文件 = 战略 → 任务 的落地解码）
> 承接：`STRATEGY_SYSTEM_ROADMAP.md`（路线 A 原生赋能者）· `STRATEGIC_CONSOLIDATION.md`（战略收口）· `PRODUCT_LANDING_PLAN.md`（产品落地蓝图）· `COMPETITIVENESS_MATURITY_SYNTHESIS.md`（二十强 CCI）
> 铁律延续：韧性降级 / 事实锚点（只调指令不改写业务数字）/ 人审护栏 / 分段开源

---

## 0. 战略锚点（为什么是这个顺序）

战略已锁三条硬约束，直接决定开发优先级：

1. **路线 A 原生赋能者**：不推倒 ERP，给客户叠加「实时决策脑 + 全息真相源」。→ 网关实时流是「最撬动全局」的入口，必须第一个做。
2. **自主可控 Z 轴（A=1.0）**：Apache-2.0 全开源是护城河楔子。→ 所有基座代码必须可自部署、可审计，不依赖闭源服务。
3. **二十强 CCI 启示**：巨头的封顶项是「闭源 + retrofit」，我们的增长项是「开放网络引力 N」。→ 每版本只补最撬动全局的一环，用「可用」换「引力」，不堆功能。

**北极星**：客户经营决策「实时化率」（由实时孪生驱动、含实时字段的决策数 ÷ 总决策数）。当前 ≈ 0%，MVP 目标 ≥ 40%，稳态 ≥ 85%。

---

## 1. 版本任务表（v21.0 → v23.0）

每个版本 = 一个可演示、可售卖的「长大一步」。验收标准可机器断言。

| 版本 | 主题 | 核心任务 | 验收（机器可断言） | 商业含义 |
|---|---|---|---|---|
| **v21.0**（进行中） | 实时孪生引擎 | ① agent 消费 twin_context 出实时结论（energy_carbon 端到端）② UNS 五路归一（含 collab） | 模拟/真实流驱动 agent 出含 `real_time_*` 字段结论；全量测试零回归；UNS 五路事件可查 | 首个真客户 demo 可跑端到端 |
| **v21.5** | 隐性捕获 | 经验库扩 人/社交/会议/协作 + 抽取即锚定 | 隐性信号经审批进经验库 ≥ 阈值；锚定率 ≥ 95% | 对手零覆盖的差异化壁垒 |
| **v22.0** | 蓝弧闭环 | 执行后果显式回流认知层 + 后果校验 | 蓝弧回流链路贯通；后果校验不阻断 | 活循环真转，自进化有燃料 |
| **v22.5** | 孪生大屏（本体为符号主义支柱） | 「智衍工业智能体互联平台」平台名 + 孪生大屏可视化（本体是三主义之一，超越 Palantir 纯符号框架） | 大屏可演示；对标 Palantir 有画面 | 销售利器 |
| **v23.0** | 自进化成熟 | P2 广样本反思 + 薄/厚 holon 治理面板 | 策略改进数/季可度量；治理面板可用 | 客户越用越聪明，续费锚 |

**节奏**：约每季度一大版本，每版本只补最撬动全局的一环（有机落实，不堆功能）。

---

## 2. 阶段 1 下半场任务拆解（本次启动项 · v21.0 起点）

> 上半场已让孪生体「有状态、可读」（twin_feed + `twin_context()` 就绪）。下半场让 agent「用」它——孪生社会真正活起来的最小一步。

| ID | 任务 | 落点 | 状态 |
|---|---|---|---|
| **T1** | 能耗孪生体 DataSource | 新增 `EnergyTwinDataSource`（TIMESERIES/MACHINE），config 默认租户引导注册，网关实时流经 `route_event` 汇入 | ✅ 已启动 |
| **T2** | energy_carbon 消费孪生体 | `analyze()` 调用 `twin_context()` 融合实时能耗/功率/绿电比，产出含 `real_time_*` 字段结论 | ✅ 已启动 |
| **T3** | 韧性降级 | 无孪生流回退 seed；孪生超期标 `stale`；异常静默不阻断 | ✅ 已启动 |
| **T4** | 端到端验证 | `scripts/verify_twin_reasoning.py`：模拟流 → 断言实时字段 → 韧性降级 | ✅ 已启动 |
| **T5** | UNS 五路归一 | 轻量统一事件总线，五路事件同 schema（gateway/system 先；human/social/meeting/collab 后） | ✅ 已启动 |

**T1–T5 验收标准**（本提交目标）：
- [x] `energy_carbon.analyze()` 在模拟流下产出含 `real_time_*` 字段结论
- [x] 网关/孪生不可达时回退 seed / 孪生空值，agent 不崩
- [x] **UNS 五路事件同 schema 归一入总线，各 channel 可查可回溯；gateway 路自动路由孪生体**
- [x] 全量测试零回归（目标 ≥ 131 passed）
- [x] demo 脚本可一键复现端到端（verify_twin_reasoning + verify_uns）

---

## 3. 实时孪生融合约定（接口契约）

为让「网关流 → 孪生体 → agent 推理」可复现、可测，定义能耗孪生 tag 约定：

- 网关/外部实时事件经 `registry.route_event("machine", values)` 上行，`values` 采用扁平键：
  - `energy_kwh__<line_id>`：实时周能耗（kWh）
  - `power_kw__<line_id>`：实时功率（kW）
  - `green_ratio__<line_id>`：实时绿电比例（%）
- `energy_carbon.analyze()` 扫描所有 `machine` holon 孪生体，解析上述键，**覆盖**种子基线对应字段，并重算 `carbon_t`，标记 `real_time=True` + `twin_source` + `updated_at`。
- 新鲜度：`updated_at` 距今 < 300s 视为 `fresh`，否则标 `stale`（仍用，但结论文案提示偏旧）。
- 无孪生数据 / 解析失败 → 整体回退种子基线，结论标注「无实时孪生流，使用种子基线」。

> 事实锚点纪律：孪生体只接收状态上行、绝不发起业务动作或改写业务数字；agent 仅「读」镜像。

---

## 4. 测试与发布纪律

- **零回归**：每次改动跑全量 `pytest tests/ -q`，目标 100% 通过。
- **e2e 脚本**：`scripts/verify_*.py` 用 `httpx.ASGITransport` 直打 app（不触发 lifespan），脚本头 `sys.path.insert(0, project_root)`。
- **韧性护栏**：外部不可达自动回退；事实锚点（ tuner 只调阈值）；人审护栏（prompt/事实/经验先入 draft，审批才生效）。
- **分段开源**：本地积累 commit，成熟后分段推 GitHub；默认不自动发布对外物料。
- **运行环境**：项目 `.venv`（`E:/agent_industry/zhiyan/.venv/Scripts/python.exe`），托管 `envs/default` 原生模块损坏不可用。

---

## 5. 决策登记（待杜先生拍板）

1. 路线 A（原生赋能者）是否正式签字？（战略已内化，待正式落章）
2. 平台对外名称 = **「智衍工业智能体互联平台」**（本体 / 符号主义仅作三主义之一的内置理念，非平台名）—— **已拍板：平台名沿用，本体升格为理念构成之一，定位超越 Palantir 纯符号框架**。
3. 公众号草稿（全息孪生社会 / 二十强图谱）是否发布？是否升格白皮书？
4. **阶段 1 下半场（v21.0 起点）已落地** —— 本提交落地 T1–T5（能耗孪生体 + energy_carbon 实时消费 + 韧性降级 + 端到端验证 + UNS 五路归一），v21.5(隐性捕获) + v22(蓝弧) 待续推。
5. 首客场景是否定为「设备健康 / 能耗孪生」？（推荐：最短 ROI、数据最干净、最易 demo）

---

*本计划是 v20.6 之后产品开发的可执行解码，实施须遵循 §4 铁律与护栏。所有代码落地沿用分段开源，所有对外物料默认杜先生终审。*
