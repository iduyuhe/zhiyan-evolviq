# 智衍 EvolvIQ · 总执行计划（Master Execution Plan）

> **状态**：2026-07-27 杜总定调 **「接受建议，按部就班、有条不紊推进」**——"先讨论再行动"治理期结束，自本文起转入按计划执行期。
> **本文地位**：唯一的执行总纲。`NEXT_STEPS_DISCUSSION.md` 使命完成转为讨论存档；各专项计划（`ENVIRONMENT_PERCEPTION_PLAN.md`、`FIRST_CUSTOMER_PILOT.md`、`SOCIAL_CHANNEL_SETUP.md`、`ECOSYSTEM_LAUNCH.md`）作为本文对应阶段的细案。

---

## 0. 治理原则（每一步都受这四条约束）

1. **🔴 白屏零容忍（战略底线，优先于一切）**：任何上线动作必过 `smoke_check.py` 冒烟门禁；新前端组件必须在 ErrorBoundary 内；未通过即视为部署失败回滚。
2. **单主线推进**：同一时间只推进一个主线阶段，做完、验收、汇报，再进下一阶段。纯文档/起草类工作可随时穿插，不算主线。
3. **每阶段三件套验收**：全量测试零回归 + 生产冒烟门禁 PASS + 向杜总一页汇报（做了什么/验证结果/下一步）。
4. **用户动作提前告知**：任何需要杜总出资源的节点（密钥/DNS/选企业），至少提前一个阶段列明，绝不临时伸手。

---

## 1. 已定调决策登记（截至 2026-07-27）

| # | 决策 | 定调内容 |
|---|---|---|
| 1 | 白屏=战略断联铁律 | 三层防御+部署冒烟门禁，已上线生产（commit 104a45b） |
| 2 | F 环境感知第⑥路 | 正式写入战略路线图（五路→六路）；治理机制=官方为锚+非官方必筛+筛选客户自定义+智能推荐 |
| 3 | F 首批外部源 | **政策法规 + 原材料行情 + 行业智能化对标**（三类，含 G5 轨道一） |
| 4 | G「外感知先行」商业模式 | 采纳为正式商业模式（获客楔子/四级信任爬梯）；**与 v30 α 合并排期**（同一套代码两种包装） |
| 5 | G5 内置感知类目 | 双轨分离落地：轨道一「行业智能化对标」=真情报走 environment 路；轨道二「平台建议」=透明标注 `platform_insight`，靠相邻呈现不靠伪装 |
| 6 | G 免费额度与付费线 | **采用建议默认值先行**（见 §3 S2），正式对外发布前可商务调整 |
| 7 | A1 接入架构方向 | 按推荐方案：**火山本地前端 + 加密隧道回源旧 43**，替代纯公网反代；启动时点见协同线 P1 |
| 8 | G 模式正式命名=**「无感转型」**（2026-07-27 杜总提出） | 公开信息驱动企业逐步解锁内部功能：外向智能体（战略/客户/市场/采购/供应链）先行，有序牵引整体智能化体系推进——企业在日常使用中完成转型，全程无"转型工程"之感。落法见 §3.5 三圈解锁模型 |

---

## 2. 全景：三条线并行结构

```
主线（我可独立推进，串行）
  S1 v30.0 α 环境感知内核        S2 v30.5 β SaaS化+自定义筛选     S3 v31 γ 智能推荐
  ├ UNS environment 路           ├ 租户自定义筛选规则+UI          ├ 源推荐（租户画像）
  ├ credibility 分级门           ├ 外感知先行免费版（G 包装）      ├ 相关性打分降噪
  ├ 三类官方源适配器             ├ G5 轨道二 platform_insight     ├ 采纳/驳回反哺（蓝弧同构）
  └ agent 消费环境信号           └ 免费额度限额                   └ 孪生大屏「体外感知」视图

协同线（需杜总动作解锁，随时可与主线并行）
  P1 A1/A2 子域名HTTPS+端口收敛 ──→ P2 C 社交通道生产接入 ──→ P3 D 首客试点
     （需：DNS解析）                 （需：企微/钉钉/邮件密钥）      （需：选定试点企业+场景）

常青线（不阻塞任何人，穿插进行）
  B 生态运营（GitHub Issue/Discussion 响应、对外叙事文章起草）
  E 对外物料（一页纸/demo视频/i18n，随主线成果逐步补齐）
```

**排序逻辑**：环境感知不依赖 A1（纯出站、无入站回调），且它同时是 v30 内核与 G 获客楔子的载体——所以主线从 S1 起步，火力最集中、外部依赖为零。协同线按"基础设施→通道→客户"顺序，每一环等杜总资源到位即启动，不阻塞主线。

---

## 3. 主线阶段详案

### S1 · v30.0 α 环境感知内核（当前阶段，即将启动）

**目标**：六路感知从纸面变成代码——外部世界信号第一次流进真相源并被 agent 引用。

| 任务 | 内容 | 细案参照 |
|---|---|---|
| S1-1 | UNS 新增 `environment` 路（channel 枚举+schema 增 `credibility` 字段） | ENVIRONMENT_PERCEPTION_PLAN §α |
| S1-2 | credibility 分级门：official 直接锚定；authoritative/general 一律进 `_needs_review` | 同上 |
| S1-3 | 三类源适配器（stdlib/httpx，出站抓取+解析归一）：①政策法规（部委公开发布）②原材料行情（官方指数）③行业智能化对标（工信部试点示范名录/灯塔工厂等公开名单） | 同上 + G5 轨道一 |
| S1-4 | `supply_chain` agent 消费环境信号，产出**含环境引用与来源溯源**的决策结论 | 同上 |
| S1-5 | 测试：新增环境感知专项测试 + 全量零回归（基线 216 passed） | — |

**验收**：demo 环境里一条真实政策/行情信号走完"抓取→UNS→分级门→锚定→agent 引用→溯源展示"全链路；测试全绿；生产部署后冒烟门禁 PASS。
**明确不做**：UI 配置面板、智能推荐、非官方源、SaaS 计费——全部留给 S2/S3，防止 α 膨胀。

### S2 · v30.5 β SaaS 化 + 租户自定义筛选（G 模式落地）

**目标**：同一套内核的第二种包装——「外感知先行」免费版可对外给企业试用。

- **S2-1** 筛选规则模型+配置 API+前端面板（复用「先测试后保存」闸门范式）。
- **S2-2** G5 轨道二：`platform_insight` 类型+界面明示「来自智衍平台的建议」，与情报相邻呈现。
- **S2-3** 免费额度限额（建议默认值，发布前可调）：**免费=3 个信息源订阅 / 每日信号 50 条 / agent 解读每月 30 次 / 单租户 1 用户**；**付费线=信任爬梯第③级**（接入第 1 个内部数据源，如上传 BOM 做「外部信号×自家数据」），进入报价流程。
- **S2-4** 信任爬梯③的最小体验：BOM/物料清单上传 → 行情信号×BOM 的毛利影响测算（价值跳变演示）。
- **S2-5** 注册/租约流程走现有多租户+JWT 体系，不新造轮子。

**验收**：一个新企业租户从注册→订阅 3 类源→收到 agent 解读→看到行业对标+平台建议（透明标注）→上传 BOM 看到价值跳变，全程无白屏、无越权。

### S3 · v31 γ 智能推荐 + 自进化闭环

推荐体系四层（第④层为 2026-07-27 杜总补充定调）：
1. **源推荐**：按租户行业/物料/供应商画像推荐值得订阅的信息源。
2. **相关性打分降噪**：信号与本企业产品/供应链/市场的关联度打分，高相关优先推给对应 agent。
3. **采纳/驳回反哺**：客户对推荐的采纳/驳回行为回流推荐模型（与蓝弧后果回流同构）。
4. **🆕 智能体推荐（无感转型导航器）**：根据客户的**使用习惯与关注重点**推荐下一个值得解锁的智能体——
   - **信号来源**（全部为租户自身产品内行为，不引入外部画像）：高频查看的情报类目（政策/行情/竞品/供应链）、agent 会话主题分布、情报采纳记录、停留与回访模式、已解锁 agent 的使用深度。
   - **推荐逻辑**：关注重点 → 三圈地图上的最短价值路径。例：反复看原材料行情 → 推 `cost_analysis`（上传 BOM 即见毛利影响）；高频查供应链风险 → 推 `demand_order`；常读政策合规 → 推 `eco_change`/`ipc_standard`。推荐语必须以"你关注的 X，配合 Y 能算出 Z"的价值句式呈现，绝不用功能列表句式。
   - **呈现纪律**：走 G5 轨道二 `platform_insight` 透明标注 + 相邻呈现，融入「解锁进度」三圈视图（在客户当前圈层旁点亮"推荐下一步"），不弹窗、不打断。
   - **闭环**：推荐→点击→解锁→留存 全链路埋点，与 §3.5 纪律 3 的转化事件同一套数据，推荐效果自度量自进化。
   - **隐私边界**：行为画像仅存于本租户、仅用于本租户推荐，绝不跨租户聚合到个体可识别粒度。
- 孪生大屏新增「体外感知」视图。
- 启动条件：S2 上线且至少有种子租户产生采纳/驳回行为数据。

### §3.5 「无感转型」三圈解锁模型（G 模式总纲，2026-07-27 杜总定调）

**核心命题**：企业不做"转型工程"，而是被公开信息牵引着逐个解锁智能体——转型在日常使用中自然完成，全程无感。四级信任爬梯升维为按 agent 阵容落位的三圈解锁地图（现有 20 agent 全覆盖，零新建）：

| 圈层 | 数据依赖 | 智能体（现有阵容落位） | 商业状态 |
|---|---|---|---|
| **外圈** | 仅公开信息（六路之⑥environment），零内部数据 | `executive_cockpit`（战略）、`supply_chain`（供应链风险）、`procurement_manage`（采购行情）、`compliance_q`（政策合规） | 免费即用（爬梯①②），SaaS 零部署 |
| **中圈** | 外部信号 × 轻量内部数据（上传 1 个文件，不接系统不进内网） | `cost_analysis`（行情×BOM=毛利影响）、`demand_order`、`bom_selector`（替代料）、`rd_npi`（技术趋势×在研项目） | **付费线**（爬梯③价值跳变） |
| **内圈** | 深度内部数据（网关/MES/全六路） | `aps_scheduler`、`oee_optimizer`、`pm_maintenance`、`energy_carbon`、`quality_trace`、`yield_analysis`、`aoi_judge`、`smt_changeover`、`wms_logistics`、`eco_change`、`dfm_check`、`ipc_standard` | 私有化部署（爬梯④），此时转型已完成 |

**三条实施纪律**：
1. **解锁由价值驱动，不由销售驱动**——中圈/内圈入口只在外圈情报旁"相邻呈现"（复用 G5 轨道二 `platform_insight` 透明标注机制），绝不弹窗推销。**解锁次序由客户行为指路**：S3 第④层「智能体推荐」根据使用习惯与关注重点推荐下一个智能体（2026-07-27 杜总定调），系统读行为、不猜需求。
2. **外圈四 agent 必须能在纯 environment 信号下产出可用结论**（S1 验收标准补充项：`executive_cockpit`/`procurement_manage`/`compliance_q` 与 `supply_chain` 同批接入环境信号消费）。
3. **每次解锁 = 一次可度量的转化事件**（埋点：外圈活跃→上传首个文件→接入首个数据源→私有化询价），漏斗数据反哺 S3 推荐模型。

**对 S1/S2 的增量影响**：S1 环境信号消费 agent 从 1 个（supply_chain）扩为外圈 4 个（工作量可控：同一消费接口挂 4 个订阅）；S2 免费版界面按三圈地图组织「解锁进度」视图——让企业自己看见"我已走到哪一圈"，差距感与获得感并存。

---

## 4. 协同线阶段详案（需杜总动作）

### P1 · A1/A2 子域名 HTTPS + 端口收敛（✅ P1-A 已完成；A2 稳定观察后收敛）

- **2026-07-27 完成**：DNS `zhiyan.weomnitech.com.cn → 115.191.20.92` 生效；火山节点部署本地前端静态副本；API 通过受限密钥的 systemd SSH 隧道回源旧 43；Let's Encrypt HTTPS 已签发并启用自动续期。
- **正式入口**：`https://zhiyan.weomnitech.com.cn`；HTTP 自动 301 到 HTTPS。公网/API/真实 Chromium 三重验收通过，白屏冒烟门禁 PASS。
- **当前架构**：静态前端在火山本地直出，API 仅经 `127.0.0.1:13006` 加密隧道回源；隧道密钥限制为只能转发旧机 `127.0.0.1:3006`，不能执行远程命令；nginx 与隧道均开机自启。
- **白屏底线保障**：进入双入口并行观察期，`43.153.172.52:3006` 原入口继续作为战略兜底，不做破坏性切换。
- **A2 待办**：新域名稳定观察通过后，再把旧 43 的 3006 收敛为仅允许火山访问；执行前必须再次通过新域名白屏门禁并保留可验证回滚路径。

### P2 · C 社交通道生产接入（HTTPS 前置已满足，待杜总提供密钥）

- **🙋 杜总动作**：企微/钉钉后台生成应用密钥（`SOCIAL_CHANNEL_SETUP.md` 有逐步指引），邮件通道给一个专用 IMAP 账号。
- 我方：回调 URL 配 `https://zhiyan.weomnitech.com.cn/api/connectors/...`，打开 `enabled`，验证信号入 UNS `social` 路。

### P3 · D 首客试点

- **🙋 杜总动作**：拍板场景（推荐 A：设备健康/能耗孪生）+ 选定 1 家试点企业 + 现场网关配合方。
- **与 G 模式联动**：S2 上线后，「外感知先行免费版」即是给试点企业（和更多潜在客户）的敲门砖——先免费用外感知建立信任，再谈内部接入，两条获客路径合流。
- 验收：`ZHIYAN_DEMO_DATA=0`，真实信号驱动决策，**决策实时化率从 0% 起跳**（北极星埋点截图为证）。

---

## 5. 常青线（穿插进行，我承担，您终审）

- **B 生态**：每周巡一次 GitHub Issues/Discussions 并响应；对外叙事文章（「开放系统的活循环」六路感知版）起草后交您终审——30 天内目标 ≥1 外部贡献者 PR。
- **E 物料**：对外一页纸（含 G 模式信任爬梯图）随 S1 完成后更新；demo 视频待 S2 免费版界面就绪后录制（画面更有说服力）。

---

## 6. 杜总动作汇总表（全计划仅这四件需要您）

| 时点 | 动作 | 解锁 |
|---|---|---|
| 随时（越早越好） | DNS 加 A 记录：`zhiyan` → `115.191.20.92` | P1 → P2 |
| P1 完成后 | 企微/钉钉密钥 + IMAP 账号 | P2 |
| 不迟于 S2 完成 | 拍板首客场景 + 试点企业 | P3 |
| S2 发布前 | 确认/调整免费额度与付费线默认值 | G 对外发布 |

---

## 7. 汇报与变更机制

- **阶段完成汇报**：每个 S/P 阶段收尾，一页汇报（做了什么/验证证据/偏差/下一步），您确认后进入下一阶段。
- **计划变更**：任何优先级调整以本文为准修订并 git 留痕；新想法先进 `NEXT_STEPS_DISCUSSION.md` 存档区讨论，定调后并入本文。
- **红线不议**：白屏门禁、事实锚点（调参不改业务数字）、F4 可信治理（广告不伪装成情报）在任何变更中不可豁免。

---

*制定：2026-07-27。依据：杜总同日定调"接受建议，按部就班推进"。*
